import React from 'react'
import styles from './Embed.module.css'

export const Embed: React.FC<any> = (props) => {
  return (
    <div className={styles.embed}>
      {/* Block Component: Embed */}
      <h2>Embed</h2>
    </div>
  )
}
