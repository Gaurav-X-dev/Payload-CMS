import React from 'react'
import styles from './Split.module.css'

export const Split: React.FC<any> = (props) => {
  return (
    <div className={styles.split}>
      {/* Block Component: Split */}
      <h2>Split</h2>
    </div>
  )
}
